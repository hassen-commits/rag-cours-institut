# Streamlit Cloud secrets — copy these values into Streamlit Cloud's
# "Secrets" UI for your deployed app (Settings → Secrets).
# Locally, prefer .env (loaded by python-dotenv); this file is only used
# if you also want to test the st.secrets path.

OPENAI_API_KEY = "sk-..."

CHAT_MODEL = "gpt-4o-mini"
EMBEDDING_MODEL = "text-embedding-3-small"
OCR_MODEL = "gpt-4o"
HYDE_MODEL = "gpt-4o-mini"

CHROMA_DIR = "./chroma_db"
COLLECTION_NAME = "cours"

CHUNK_SIZE = "1000"
CHUNK_OVERLAP = "150"
TOP_K = "5"
HYDE_ENABLED = "1"
